#include<bits/stdc++.h>
using namespace std;
const float pi = 3.14;
int main()
{
    float a;
    cin >> a;
    cout << fixed << setprecision(3) << 2 * pi * a << " " << pi * pow(a, 2);
    return 0;
}