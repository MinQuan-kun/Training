#include<bits/stdc++.h>
using namespace std;
int main()
{
    float a, b;
    cin >> a >> b;
    if((a > 0 && b > 0) || (a < 0 && b < 0))
        cout << "YES";
    else if(a == 0 || b == 0)
        cout << 0;
    else
        cout << "NO";
    return 0;
}