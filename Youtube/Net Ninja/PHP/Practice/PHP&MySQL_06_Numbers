<?php
    $radios = 25;
    $pi = 3.14;

    // basic operations
    // echo $pi * $radios**2; (mũ 2)

    // order of operations (B I D M A S)
    // echo 2 * (4 + 9) / 3;

    // increment / decrement
    // $radios++; // $radios = $radios + 1;
    // echo $radios; // 26
    // $radios--; // $radios = $radios - 1;
    // echo $radios;

    // $age = 20;

    // $age += 5; // $age = $age + 5;
    // echo $age; // 25
    // $age -= 5; // $age = $age - 5;
    // echo $age; // 20
    // $age *= 2; // $age = $age * 2;
    // echo $age; // 40
    // $age /= 4; // $age = $age / 4;
    // echo $age; // 10
    // $age %= 3; // $age = $age % 3;
    // echo $age; // 1

    // number functions
    // echo abs(-100); // 100
    // echo pow(2, 4); // 2^4 = 16
    // echo sqrt(144); // 12
    // echo floor($pi); // 3
    // echo ceil($pi); // 4
    // echo pi(); // 3.14

?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PHP Turtorial</title>
</head>
<body>
    <h1>
        <?php 
            
        ?>
    </h1>
    
</body>
</html>